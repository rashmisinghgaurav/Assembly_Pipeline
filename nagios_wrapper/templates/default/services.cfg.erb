define service{
use                     generic-service
host_name               <%= node['nrpe']['client']['ip'] %>
service_description     SSH Monitoring
check_command           check_ssh
}
define service{
use                     generic-service
host_name               <%= node['nrpe']['client']['ip'] %>
service_description     FTP Monitoring
check_command           check_ftp
}

